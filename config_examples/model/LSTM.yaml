name: LSTM

params:
  hidden_dim: 50 #50 #200
  n_layers: 1 #2
  output_dim: 1
  dropout: 0.0 #0.05

hyperparameters_vary:
  dropout:
    optuna_type: float
    low: 0.1
    high: 0.7
    step: 0.1

attack_train_mode: True
